interface:
  display_name: "Aliyun Infrastructure Ops"
  short_description: "Safe Aliyun CLI operations, CAS, OSS and TLS"
  default_prompt: "Use $aliyun-infra to safely request or renew an Aliyun SSL certificate, deploy an existing certificate to OSS, or verify public TLS."
